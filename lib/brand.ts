/** User-facing brand (two words). */
export const BRAND_DISPLAY = "Grade Five";

/** Identifier-safe brand (no space) for code and short internal labels. */
export const BRAND_ID = "GradeFive";

/** Compact logo monogram. */
export const BRAND_MONOGRAM = "G5";
